# State files (generated, not tracked)
state.json
*.log

# Night mode ambient videos (large, deployed separately)
pi/dashboard/videos/

# macOS
.DS_Store

# Python
__pycache__/
*.pyc

# Credentials / API keys (NEVER commit)
focusboard-config.json
*.secret
*.key
*credentials*
*token.json

# Allow the safe example template
!focusboard-config.example.json

# Editor
.vscode/
*.swp
